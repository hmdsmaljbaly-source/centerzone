# 📘 مستند المواصفات والتصميم الشامل والنهائي - منصة Center Zone SaaS (`centerzone_system`)

## 1. الرؤية الهندسية والمكدس التقني (Tech Stack & Architecture)
- **اسم المنصة:** Center Zone SaaS (`centerzone_system`).
- **طبيعة المنصة:** Multi-Tenant SaaS لإدارة السناتور والمراكز التعليمية، إدارة الطلاب، المجموعات، القاعات، المخزن، نقاط البيع السريعة، الحضور بالباركوود، والماليات.
- **المكدس التقني (Tech Stack):**
  - **Backend:** Node.js (Express Framework) مستضاف على بيئات Linux السحابية (Render / Railway).
  - **Database & ORM:** Cloud PostgreSQL عبر NeonDB مع Prisma ORM.
  - **Frontend:** HTML5, Tailwind CSS (CDN), Font Awesome, Cairo Font, Vanilla JS.

---

## 2. معمارية تعدد المستأجرين والعزل الأمني الصارم (Multi-Tenancy & Security Rules)
1. **Header Handshake:** تمرير معرف السنتر في كل طلب عبر الهيدر باسم `x-center-id`.
2. **Middleware Isolation:** التحقق المباشر من المعرف ومطابقته في NeonDB وتثبيته في `req.tenantId` (UUID).
3. **Database Naming Convention:**
   - احتواء كل جدول في Prisma على `centerId String @map("center_id")`.
   - الاعتماد على camelCase في JS/Prisma وتعيين الخريطة إلى snake_case في PostgreSQL لمنع أخطاء العلاقات.
   - استخدام snake_case لكافة المفاتيح الأجنبية (`teacher_id`, `student_id`, `group_id`, `hall_id`).
4. **Data Query Isolation:** قصر جميع استعلامات قاعدة البيانات بشرط `where: { centerId: req.tenantId }`.

---

## 3. محرك التراخيص والأكواد مسبقة الدفع (Prepaid License Engine)
- **نموذج البيع:** بيع الأكواد للسناتور بحصص محددة (مثل 1000 كود).
- **البادئة الثابتة (Fixed Prefix):** تخصيص كود بادئة لكل سنتر (مثل `cen_z_`).
- **التصدير والطباعة:** إمكانية تصدير شيت Excel / PDF يحتوي رمز الكود والباركوود للطباعة المباشرة ككروت بلاستيكية.
- **التسجيل الذكي:** إدخال البادئة تلقائياً في واجهة التسجيل والتحقق اللحظي من صلاحية الكود وتغيير حالته إلى "مستعمل" فور التسجيل.

---

## 4. لوحة السوبر أدمن وإدارة الصلاحيات (Super Admin & RBAC Engine)
- **السلطة المركزية:** السوبر أدمن هو المسئول الوحيد عن إنشاء حسابات مستخدمي السنتر (صاحب السنتر، الاستقبال، المساعدين) وتعيين أسماء المستخدمين وكلمات السر.
- **شجرة الصلاحيات التفاعلية (Granular Permissions):** تحديد الشاشات والـ APIs المصرح بها لكل موظف (الماليات، المخزن، الحضور، المجموعات، الخ).
- **العزل الجانبي (Front & Back Security):** إخفاء التبويبات غير المصرح بها في الواجهة وإرجاع `403 Forbidden` من السيرفر عند محاولة وصول غير مصرح.

---

## 5. غرفة عمليات المجموعات والتقييمات المباشرة (Group Session Hub - `scanner.html`)
- **اختيار المجموعة:** تحميل بيانات طلاب المجموعة المختارة فقط.
- **كروت الإحصائيات اللحظية:** إجمالي عدد الطلاب، عدد الحضور، الغياب، ونسبة الحضور.
- **جدول الحضور والتسميع التفاعلي:**
  - مسح الباركوود يغير حالة الطالب لحظياً إلى **[حضر 🟢]** مع الخصم التلقائي للحصة، أو ترك الحالة **[غائب 🔴]**.
  - شريط التقييمات السريعة: إمكانية إدراج تسميع/امتحان جديد وتحديد الدرجة النهائية.
  - مربع إدخال درجات سريع لكل طالب في القائمة مع تسجيل كلمة "غائب" للطلاب الغائبين تلقائياً.

---

## 6. صفحة بروفايل الطالب المستقلة والمعالجة الشهرية (`student-profile.html`)
- **صفحة منفصلة بالكامل:** تضم البيانات الشخصية، الكود (`cen_z_...`)، السجل الزمني للحضور والغياب، سجل الدرجات والتسميعات، والسجل المالي والدفعات.
- **الحساب الذكي للشهر والحصص:** ربط سعر الشهر بعدد الحصص أثناء إنشاء المجموعة واقتراح قيمة الاشتراك تلقائياً عند الدفع وشحن الحصص المحددة.
- **نظام الخصومات:** تطبيق خصم نسبة مئوية `%` أو مبلغ ثابت `EGP` مع تسجيل سبب الخصم وتوريد الصافي للخزينة.

---

## 7. سريان حركة البيانات (End-to-End Data Flow Execution)

### أ. سيناريو التسجيل بكارت الاشتراك (`cen_z_1001`):
1. إدخال كود الطالب في الواجهة ⬅️ الـ Interceptor يضيف هيدر `x-center-id`.
2. الـ Middleware يتحقق من صلاحية الجلسة ومعرف السنتر `req.tenantId`.
3. معاملة ذرية `prisma.$transaction`:
   - فحص جدول الكروت `CenterPrepaidCard`: التأكد أن الكود يخص السنتر وحالته "UNUSED".
   - تحديث حالة الكود إلى "USED" وربطه بالطالب.
   - إنشاء حساب الطالب `Student` برصيد حصص `0` ورقم باركوود مطابق.

### ب. سيناريو الحضور والتسميع المباشر (`scanner.html`):
1. مسح باركوود الطالب ⬅️ الـ API يفحص وجود الطالب ورصيده.
2. خصم حصة واحدة تلقائياً من `remainingSessions` وتسجيل حضور في `StudentAttendance`.
3. تحديث شارة الطالب فوراً في الواجهة إلى **[حضر 🟢]** وتحديث عداد الحضور والغياب لحظياً.

---

## 8. خريطة الهيكل والملفات المعتمدة للمشروع (Project Structure Blueprint)

```text
centerzone_system/
├── .env                              <-- إعدادات بيئة العمل ورابط NeonDB
├── package.json                      <-- مكتبات Node.js و Express و Prisma
├── SYSTEM_MASTER_PLAN_AR.md          <-- (هذا الملف المرجعي الشامل)
├── prisma/
│   └── schema.prisma                 <-- المخطط الشامل للجداول المعزولة بـ center_id
├── config/
│   └── prisma.js                     <-- الاتصال الموحد بـ Prisma Client
├── middleware/
│   ├── tenant.middleware.js          <-- استخراج واستوثاق x-center-id
│   ├── auth.middleware.js            <-- التحقق من الـ JWT Token
│   └── rbac.middleware.js            <-- فحص صلاحيات الموظف
├── controllers/
│   ├── superadmin.controller.js      <-- منطق الأكواد وتوليد السنترات والحسابات
│   ├── student.controller.js         <-- منطق الطلاب والبروفايل والدفع والخصم
│   ├── attendance.controller.js     <-- منطق الحضور والتسميع والامتحانات
│   ├── pos.controller.js            <-- منطق POS والمخزن وتوريد الملازم
│   └── financials.controller.js     <-- منطق الخزينة والمصروفات وتقفيل الوردية
├── routes/
│   ├── superadmin.routes.js          <-- مسارات السوبر أدمن والصلاحيات
│   ├── student.routes.js             <-- مسارات الطلاب والبروفايل
│   ├── attendance.routes.js          <-- مسارات الحضور والتسميع
│   ├── pos.routes.js                 <-- مسارات المبيعات والمخزن
│   └── financials.routes.js          <-- مسارات الماليات وتقفيل الوردية
├── frontend/ (أو public/)
│   ├── js/app-core.js                <-- المحرك الموحد للـ Fetch Interceptor
│   ├── login.html                    <-- تسجيل الدخول
│   ├── super-admin.html              <-- السوبر أدمن والأكواد والصلاحيات
│   ├── index.html                    <-- الداشبورد الرئيسية والتنبيهات المبكرة
│   ├── scanner.html                  <-- غرفة الحضور والتسميع المباشر
│   ├── student-profile.html          <-- بروفايل الطالب المستقل
│   ├── students.html                 <-- كشف الطلاب والشحن
│   ├── teachers.html                 <-- المدرسين والحصص وحارس التضارب
│   ├── inventory.html                <-- المخزن والـ POS
│   ├── financials.html               <-- الماليات وتقفيل الوردية
│   └── settings.html                 <-- الإعدادات والواتساب
└── server.js                         <-- سيرفر Express الرئيسي والـ Middlewares
```
